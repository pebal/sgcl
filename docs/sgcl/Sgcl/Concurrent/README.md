# Sgcl::Concurrent

Structures shared by any number of threads without a lock: the textbook algorithms with no reclamation scheme in them, because the collector is one, with the interfaces of `java.util.concurrent` and `System.Collections.Concurrent`; the atomic `Ptr` they are built on; a value replaced whole; a cache over the hash dictionary. `#include "sgcl/Sgcl/Concurrent/Concurrent.h"` brings the module in; it is a facade over [`sgcl/concurrent/`](../../concurrent/README.md), whose README is the guide of the module (what the classes are, the rules, what to reach for), the same under these names and depends on [`Core`](../Core/README.md) and [`Containers`](../Containers/README.md). The index of the whole interface is [`docs/sgcl/Sgcl/`](../README.md).
Structures shared by any number of threads without a lock: the textbook algorithms with no reclamation scheme in them, because the collector is one, with the interfaces of `java.util.concurrent` and `System.Collections.Concurrent`; the atomic `Ptr` they are built on; a value replaced whole; the persistent list, dictionary and set of Clojure and Scala, whose versions share their nodes. `#include "sgcl/Sgcl/Concurrent/Concurrent.h"` brings the module in; it is a facade over [`sgcl/concurrent/`](../../concurrent/README.md), whose README is the guide of the module (what the classes are, the rules, what to reach for), the same under these names and depends on [`Core`](../Core/README.md) and [`Containers`](../Containers/README.md). The index of the whole interface is [`docs/sgcl/Sgcl/`](../README.md).

| page | header | what it is |
|---|---|---|
| [Atomic, AtomicRef](Atomic.md) | `Atomic.h` | lock-free atomic `Ptr`: `Load`, `Store`, compare-exchange, `Wait`/`Notify`, no ABA ([AtomicRef](AtomicRef.md) on its own page) |
| [ConcurrentQueue](ConcurrentQueue.md) | `ConcurrentQueue.h` | the Michael–Scott queue: unbounded, FIFO, `Enqueue`, `TryDequeue`, a blocking `Dequeue` |
| [ConcurrentStack](ConcurrentStack.md) | `ConcurrentQueue.h` | the Treiber stack: one word, `Push`, `TryPop`, a blocking `Pop` |
| [ConcurrentBoundedQueue](ConcurrentBoundedQueue.md) | `ConcurrentBoundedQueue.h` | Vyukov's bounded MPMC queue: a ring of cells with sequence numbers, one compare-exchange per operation, no allocation per element, `TryEnqueue`, `TryDequeue`, a blocking `Enqueue` and `Dequeue` |
| [SpscQueue](SpscQueue.md) | `SpscQueue.h` | a ring with a sequence per cell for one producer and one consumer, the cell the one line the two share: wait-free, no compare-exchange, the same interface |
| [ConcurrentPriorityQueue](ConcurrentPriorityQueue.md) | `ConcurrentPriorityQueue.h` | a binary heap under a spin-then-park lock, what Java's `PriorityBlockingQueue` is (the skip-list one lost to it): the least element first, equal ones FIFO, `Enqueue`, `TryDequeue`, a blocking `Dequeue`, `TryPeek` |
| [ConcurrentSortedDictionary](ConcurrentSortedDictionary.md) | `ConcurrentDictionary.h` | the lock-free skip list of Herlihy and Shavit: an ordered dictionary with `Find`, `TryGet`, `Add`, `GetOrAdd`, `Remove`, weakly consistent iteration |
| [ConcurrentSortedSet](ConcurrentSortedSet.md) | `ConcurrentHashSet.h` | the same skip list with the value as the element |
| [ConcurrentDictionary](ConcurrentDictionary.md) | `ConcurrentDictionary.h` | the split-ordered list of Shalev and Shavit: a lock-free hash dictionary that doubles its bucket array without moving a node |
| [ConcurrentHashSet](ConcurrentHashSet.md) | `ConcurrentHashSet.h` | the same table with the value as the element |
| [ConcurrentCache](ConcurrentCache.md) | `ConcurrentCache.h` | a cache over the hash dictionary bounded by a capacity and a time to live, the least recently used evicted by sampling: `TryGet`, `Set`, `GetOrAdd`, `Hits`, `Misses` |
| [ConcurrentWeakDictionary](ConcurrentWeakDictionary.md) | `ConcurrentWeakDictionary.h` | the WeakDictionary shared by any number of threads: values attached to objects the dictionary does not keep alive, over the lock-free hash table, the dead entries swept by the inserting threads |
| [ConcurrentWeakHashSet](ConcurrentWeakHashSet.md) | `ConcurrentWeakDictionary.h` | the same table with the objects alone: a set of objects it does not keep alive, shared by the threads |
| [CopyOnWrite](CopyOnWrite.md) | `CopyOnWrite.h` | a value read by many threads and replaced whole: one load for an immutable snapshot, a copy and a compare-exchange for a change |
| [Intern](Intern.md) | `Intern.h` | a pool where equal values share one managed object (Go's `unique`, Java's `String.intern`): `Make(value)` the canonical object, held weakly, compared by identity; `InternString` for Strings |
| [PersistentList](PersistentList.md) | `PersistentList.h` | the persistent vector of Clojure and Scala: a 32-way trie with a tail, every `Add`, `RemoveLast` and `Set` a new version sharing all but a path |
| [PersistentDictionary](PersistentDictionary.md) | `PersistentDictionary.h` | the persistent hash dictionary: Bagwell's hash array mapped trie, every `Set` and `Remove` a new version sharing all but a path; transparent lookup |
| [PersistentSet](PersistentSet.md) | `PersistentDictionary.h` | the same trie with the value as the element |
